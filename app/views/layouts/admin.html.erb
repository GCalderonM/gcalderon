<!DOCTYPE html>
<html>
    <head>
        <title>Admin - GCalderonM</title>
        <meta name="viewport" content="width=device-width,initial-scale=1">
        <%= csrf_meta_tags %>
        <%= csp_meta_tag %>
        <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

        <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
        <%= javascript_importmap_tags %>
    </head>

    <body class="flex h-screen flex-col bg-zinc-50 dark:bg-black">
        <%= render "shared/admin_navbar" %>
        <%= turbo_frame_tag "flash_alerts" do %>
            <%= render "shared/flash_alerts" %>
        <% end %>
        <main class="flex justify-center h-full mt-12 px-2 sm:px-4 py-2.5">
            <%= yield %>
            <%= turbo_frame_tag "modal" %>
        </main>
    </body>
</html>
